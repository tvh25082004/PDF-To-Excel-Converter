[phases.setup]
nixPkgs = ["python311", "pip"]

[phases.install]
cmds = [
  "pip install --upgrade pip",
  "pip install -r ConverterApp/requirements.txt"
]

[start]
cmd = "cd ConverterApp && gunicorn pdftoexcel:app --bind 0.0.0.0:$PORT"

